@*
  // PORTAL-DOORS Project Copyright (c) 2006-2025 Brain Health Alliance. All Rights Reserved.
  // Software license: the OSI approved Apache 2.0 License (https://opensource.org/licenses/Apache-2.0).
*@

@{
  bool userAuth = (Model.QebiRcp.IsAuthenticated ?? false);
  string userName = (Model.QebiRcp.UserName ?? ESS);
  PdpSiteRazorModel psrm = Model.PSRM;
  NpdsClientWrace npdscw = Model.NPDSCW;
}

@Html.Raw(psrm.FormatBodyTitle("NpdsWebLib Index Menu"))
<p><partial name="_NexusWebLibSpanPageMenu" /></p>

@Html.Raw(psrm.FormatBodyTitle("NpdsWebLib Project Menus"))

@if (!userAuth)
{
  <p>
    NpdsWebLib Project Menus only visible in authorized mode. 
    You may
    <a asp-page="@DepqAnonModeLoginUser">login</a> or
    <a asp-page="@DepqAnonModeRegisterUser">register</a>.
  </p>
}

@if (userAuth && PDPSS.AppUseDevtestFeature)
{
  <partial name="_NexusWebLibListPageMenu" />  
}

<h1>H1 Section Title</h1>
<h2>H2 Section Title</h2>
<h3>H3 Section Title</h3>
<h4>H4 Section Title</h4>
<h5>H5 Section Title</h5>
<h6>H6 Section Title</h6>

